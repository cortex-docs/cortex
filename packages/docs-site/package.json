{
  "name": "@cortex/docs-site",
  "version": "0.1.0",
  "private": true,
  "description": "Product documentation for Cortex Docs",
  "scripts": {
    "dev": "cortex docs serve --port 3200",
    "build": "cortex docs build --output .cortex/docs",
    "clean": "rm -rf .cortex"
  },
  "dependencies": {
    "@cortex/cli": "0.1.0"
  }
}
