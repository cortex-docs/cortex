<% const ns = it.utils?.toPascalCase?.(it.config?.languageConfig?.package_name) ?? it.clientClass.replace(/JsonRpc$/, ''); -%>
using <%= ns %>;

var client = new <%= it.clientClass %>(
    url: "<%= it.serverUrl ?? it.baseUrl ?? 'http://localhost:3000/rpc' %>"
);
