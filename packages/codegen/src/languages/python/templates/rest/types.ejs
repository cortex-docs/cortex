from __future__ import annotations

from enum import Enum
from typing import Any, Optional
from pydantic import BaseModel, ConfigDict, Field

class FileUpload(BaseModel):
    """A file part sent by a multipart/form-data request."""
    filename: str
    data: bytes
    content_type: str = "application/octet-stream"


<% for (const schema of it.schemas) { %>
<% if (schema.isEnum) { %>
class <%= schema.className %>(str, Enum):
<% for (const v of schema.enumValues) { %>
    <%= String(v).replace(/[^a-zA-Z0-9_]/g, '_').toUpperCase() %> = "<%= v %>"
<% } %>

<% } else { %>
class <%= schema.className %>(BaseModel):
    model_config = ConfigDict(populate_by_name=True)
<% if (schema.properties.length === 0) { %>
    pass
<% } else { %>
<% for (const prop of schema.properties) { %>
<% if (prop.name !== prop.originalName) { %>
    <%= prop.name %>: <% if (!prop.required) { %>Optional[<%= prop.type %>] = Field(None, alias="<%= prop.originalName %>")<% } else { %><%= prop.type %> = Field(alias="<%= prop.originalName %>")<% } %>
<% } else { %>
    <%= prop.name %>: <% if (!prop.required) { %>Optional[<%= prop.type %>] = None<% } else { %><%= prop.type %><% } %>
<% } %>
<% } %>
<% } %>

<% } %>
<% } %>
