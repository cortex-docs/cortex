<% const pkgName = it.pkgName ?? it.service?.packageName ?? it.config?.languageConfig?.package_name; -%>
import { <%= it.clientClass %> } from '<%= pkgName %>';

const client = new <%= it.clientClass %>({ address: 'localhost:50051' });
