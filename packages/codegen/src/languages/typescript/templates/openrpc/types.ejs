<% for (const [name, schema] of Array.from(it.schemas.entries())) { -%>
<% const className = it.naming.className(name); -%>
<% if (schema.enum) { -%>
export type <%= className %> = <%= schema.enum.map(v => `'${v}'`).join(' | ') %>;

<% } else if (schema.type === 'object' && schema.properties) { -%>
<% if (schema.description) { -%>
/** <%= schema.description %> */
<% } -%>
export interface <%= className %> {
<% const required = schema.required ?? []; -%>
<% for (const [propName, propSchema] of Object.entries(schema.properties)) { -%>
<% if (propSchema.description) { -%>
  /** <%= propSchema.description %> */
<% } -%>
  <%= it.naming.propertyName(propName) %><%= required.includes(propName) ? '' : '?' %>: <%= it.mapJsonSchemaType(propSchema) %>;
<% } -%>
}

<% } else { -%>
export type <%= className %> = <%= it.mapJsonSchemaType(schema) %>;

<% } -%>
<% } -%>
