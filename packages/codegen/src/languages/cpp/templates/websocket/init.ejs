<% const baseUrl = it.spec.info.servers[0]?.url ?? 'http://localhost:3000'; -%>
<% const wsUrl = it.websocketUrl ?? baseUrl.replace(/^http/, 'ws').replace(/\/v\d+$/, '') + '/ws'; -%>
#include "ws_client.hpp"
#include "ws_types.hpp"

sdk::ws::<%= it.clientClass %> ws({
  .url = "<%= wsUrl %>",
});
