services:
  sdk-tests:
    build:
      context: ../..
      dockerfile: e2e/docker/Dockerfile
    environment:
      - MOCK_PORT=4010
      - TEST_LANGUAGES=${TEST_LANGUAGES:-}
    volumes:
      - /tmp/cortex-e2e-results:/results
      - cortex-sdk-gradle-cache:/root/.gradle

volumes:
  cortex-sdk-gradle-cache:
    name: cortex-sdk-gradle-cache
    external: true
